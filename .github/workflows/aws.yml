
name: Deploy to Amazon ECS

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build project
        run: mvn clean install -DsKipTests
      - name: Login Docker Hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      - name: Build docker image
        run: docker build -t daniel00dev/ci-cd-pipeline .
      - name: push image docker hub
        run: docker push daniel00dev/ci-cd-pipeline:latest
#
#  deploy:
#    needs: build
#    runs-on: self-hosted
#    steps:
#      - name: Pull image from docker hub
#        run: sudo docker pull daniel00dev/ci-cd-pipeline:latest
#      - name: Remove docker container
#        run: sudo docker rm -f api-ci-cd
#      - name: Run docker container
#        run: sudo docker run -d -p 8089:8089 --name api-ci-cd daniel00dev/ci-cd-pipeline



